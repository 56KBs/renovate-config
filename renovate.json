{
  "regexManagers": [
    {
      "fileMatch":  ["(^|/)Jenkinsfile$"],
      "matchStrings": ["image [\"'](?<depName>[0-9a-z-./]+):(?<currentValue>[a-z0-9.-]+)@(?<currentDigest>sha256:[a-f0-9]+)[\"']"],
      "datasourceTemplate": "docker"
    }
  ]
}
